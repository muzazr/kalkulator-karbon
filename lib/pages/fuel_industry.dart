import 'package:flutter/material.dart';

class FuelIndustry extends StatelessWidget {
  const FuelIndustry({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Bahan Bakar Industri'),
    );
  }
}
import 'package:flutter/material.dart';

class ElectricTool extends StatelessWidget {
  const ElectricTool({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Peralatan Listrik'),
    );
  }
}

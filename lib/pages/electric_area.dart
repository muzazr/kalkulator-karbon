import 'package:flutter/material.dart';

class ElectricArea extends StatelessWidget {
  const ElectricArea({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Konsumsi Listrik Wilayah'),
    );
  }
}